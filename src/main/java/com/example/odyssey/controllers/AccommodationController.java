package com.example.odyssey.controllers;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping(value = "/api/v1/accommodations")
public class AccommodationController {

        // GET AllAccommodations()
        // GET Accommodation(Long accommodationID)
        // GET AccommodationByHost(Long userID)
        // POST CreateAccommodation(RequestAccommodationDTO accommodation)
        // PUT UpdateAccommodation(RequestAccommodationDTO accommodation)
        // DELETE DeleteAccommodation(Long accommodationID)
}
